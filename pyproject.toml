[project]
name = "ai-help-center-source-tester"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "scikit-learn>=1.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]